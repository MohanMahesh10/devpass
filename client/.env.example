# For local dev, leave unset so the CRA proxy (in package.json) forwards /api calls to :3001
# For a split deployment (Static Web Apps + separate App Service), point this at your API origin:
# REACT_APP_API_BASE=https://devpass-api.azurewebsites.net
